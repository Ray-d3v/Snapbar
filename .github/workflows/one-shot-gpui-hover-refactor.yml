name: Disabled GPUI hover refactor helper

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled after the failed one-shot patch; source changes are applied directly."
